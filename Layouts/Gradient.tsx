import { View, Text } from 'react-native';
import React from 'react';

const Gradient = () => {
    return (
        <View>
            <Text>Gradient</Text>
        </View>
    );
};

export default Gradient;
